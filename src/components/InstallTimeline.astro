---
const timelineSteps = [
  {
    title: "Site Assessment",
    text: "We assess your roof, energy usage and system requirements."
  },
  {
    title: "System Design",
    text: "We design a compliant, efficient solar system tailored to you."
  },
  {
    title: "Installation",
    text: "Our local team installs your system quickly and cleanly."
  },
  {
    title: "Power On",
    text: "System is tested, approved and ready to start saving you money."
  }
];
---

<section class="installTimelineSection">
  <div class="installTimelineContent">
    <h2 class="installTimelineTitle">Our Installation Process</h2>

    <div class="installTimelineGrid">
      {timelineSteps.map((step, index) => (
        <div class="timelineCard">
          <span class="timelineStepNumber">{index + 1}</span>
          <h3 class="timelineStepTitle">{step.title}</h3>
          <p class="timelineStepText">{step.text}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .installTimelineSection {
    background-color: #f7f7f7;
    padding: 80px 20px;
  }

  .installTimelineContent {
    max-width: 1100px;
    margin: 0 auto;
    text-align: center;
  }

  .installTimelineTitle {
    font-size: 32px;
    margin-bottom: 50px;
    color: #333;
  }

  .installTimelineGrid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 24px;
  }

  .timelineCard {
    background-color: #ffffff;
    padding: 28px;
    border-radius: 8px;
  }

  .timelineStepNumber {
    display: inline-block;
    font-size: 20px;
    font-weight: bold;
    color: #ff9800;
    margin-bottom: 12px;
  }

  .timelineStepTitle {
    font-size: 18px;
    margin-bottom: 10px;
    color: #333;
  }

  .timelineStepText {
    font-size: 15px;
    color: #555;
  }
</style>
